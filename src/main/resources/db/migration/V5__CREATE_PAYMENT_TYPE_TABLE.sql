CREATE TABLE PAYMENT_TYPE (
   ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
   PAYMENT_NAME VARCHAR(30) NOT NULL CHECK(PAYMENT_NAME <> ''),
   CREATED_AT TIMESTAMP NOT NULL,
   UPDATED_AT TIMESTAMP NOT NULL
);