CREATE TABLE PERSON (
    ID bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    PERSON_NAME VARCHAR(30) NOT NULL CHECK(PERSON_NAME <> ''),
    CREATED_AT TIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP NOT NULL
);